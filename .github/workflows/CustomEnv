name: Custom Environment Variable

on:
  push:
    branches: [main]

jobs:
  example:
    runs-on: ubuntu-latest
    env:
      var: "Hello env variable"

    steps:
      - name: Display Environment Variable
        run: echo "This is my env variable$var "
